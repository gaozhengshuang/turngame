module game {
    export class BattleBody extends GameComponent implements PoolItem {
        battleBodyType: BattleBodyType;

        onCreate() {
        }

        onDestroy() {
        }
    }
}